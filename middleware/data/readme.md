# Data Folder

## What we will have Here

Here we will store json files for apis that we register, metrics we need for analysis, event or
notifications that we recieve on creating apis via admin routes, so that we can keep track of whats happening around. The event api keep track of everything so when someone changes or update delete or create an api we will know via events api. It is persistant over memory as it use json files to store data.
